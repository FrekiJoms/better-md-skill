# Capability check (with visual tools)

This fixture is run in an environment with screenshot capture and repository asset creation available, and the user has asked for visuals to be added.

## Preview

The preview panel shows the rendered report with a sidebar, toolbar, and canvas area.

## Usage

```bash
report-tool render input.md
```