# Reporting Tool

A tool that generates PDF reports from CSV data.

## Report viewer

The report viewer shows the generated report with a preview pane.

![Report preview](./assets/report-preview.png)

## Export

Run the export command to save a report:

```bash
reporting-tool export input.csv --format pdf
```