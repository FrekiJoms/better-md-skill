# Theme Aware

A library with theme-aware branding.

## Repository assets

- `assets/logo-light.svg`
- `assets/logo-dark.svg`

## Header

The README header should use the `<picture>` element with `prefers-color-scheme` so the logo matches the visitor's theme. The dark variant exists; only use variants that actually exist in the repository.