# Capability check (no visual tools)

This fixture is run in an environment with no screenshot capture, image generation, upload, or browser access.

## Preview

The preview panel shows the rendered report with a sidebar, toolbar, and canvas area.

## Usage

```bash
report-tool render input.md
```