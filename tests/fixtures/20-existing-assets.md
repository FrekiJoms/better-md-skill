# Asset Locator

This repository contains its own brand assets.

## Layout

- `assets/logo.svg` — primary logo
- `assets/icon.png` — favicon source
- `docs/images/architecture.png` — architecture diagram

## Header

The README header should prefer the repository's own `assets/logo.svg` over any external asset service.